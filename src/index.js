import React from 'react';
import ReactDOM from 'react-dom';
import Login from './login'; // Import your Login component
import './login.css'; // Import any CSS files if needed

ReactDOM.render(
  <React.StrictMode>
    <Login />
  </React.StrictMode>,
  document.getElementById('root')
);
